#version 450

layout (local_size_x = 16, local_size_y = 16) in;
layout(std140, binding = 0) buffer targetBuffer
{
    vec3 pixels[ ];
};

void main()
{
    int index = int(gl_GlobalInvocationID);
    pixels[index] = vec3(0.3);
}